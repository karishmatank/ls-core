def multiply(one, two):
    return f"{float(one)} * {float(two)} = {float(one) * float(two)}"

first = input("Enter the first number: ")
second = input("Enter the second number: ")
print(multiply(first, second))
